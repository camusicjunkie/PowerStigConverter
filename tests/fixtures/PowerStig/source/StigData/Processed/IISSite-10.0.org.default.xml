<?xml version="1.0" encoding="utf-8"?>
<!--
    Fixture organisational settings, answered. The conversion refuses to generate while any of
    these is blank - see docs/adr/0001 - so a fixture meant to produce a role has to fill them in.

    The value is quoted the way PowerStig quotes it upstream: xWebAppPool interpolates it into a
    resource block it builds as a string, so the quotes are that consumer's syntax and
    OrganizationData.psd1's Unquote key takes them off again.
-->
<OrganizationalSettings fullversion="10.0">
  <OrganizationalSetting id="V-218778" Value="'00:05:00'" />
</OrganizationalSettings>
